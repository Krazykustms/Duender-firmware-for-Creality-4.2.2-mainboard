## Summary

<!-- What changed and why (1–3 sentences) -->

## Type of change

- [ ] Measured machine values (`Duender-CoreXY.json`)
- [ ] CI / build tooling
- [ ] Documentation only
- [ ] Issue templates / repo meta

## Testing

- [ ] `bash scripts/ci/generate-config.sh` succeeds
- [ ] `pio run` succeeds (local or Actions)
- [ ] Flashed on hardware (if config values changed)

## Machine details (if config changed)

| Field | Value |
|-------|-------|
| Duender version | PIN v6 MGN9H |
| Probe mount | |
| `X_BED_SIZE` × `Y_BED_SIZE` | |
| `Z_MAX_POS` | |
| `NOZZLE_TO_PROBE_OFFSET` | |
| `INVERT_X_DIR` / `INVERT_Y_DIR` | |

## Upstream refs (if bumped)

- Ender3V2S1:
- Special_Configurations:
